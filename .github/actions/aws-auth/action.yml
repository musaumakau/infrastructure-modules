name: 'AWS OIDC auth'
description: 'Assume an IAM role via OIDC — no long-lived keys'

inputs:
  role-arn:
    description: 'ARN of the IAM role to assume'
    required: false
    default: 'arn:aws:iam::649203810550:role/EksOIDCRole'  
  aws-region:
    description: 'AWS region'
    required: false
    default: 'eu-west-1'    
  session-name-suffix:
    description: 'Appended to github-actions- for the session name'
    required: false
    default: '${{ github.run_id }}'

runs:
  using: composite
  steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@e1253824e5c10ff9df46874f81ed3ec929e19cfd  # v6.3.0 SHA-pinned
      with:
        role-to-assume: ${{ inputs.role-arn }}
        aws-region: ${{ inputs.aws-region }}
        role-session-name: "github-actions-${{ inputs.session-name-suffix }}" 