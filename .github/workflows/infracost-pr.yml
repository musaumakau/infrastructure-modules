name: Infracost PR Analysis
on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'eks/**'
      - 'vpc/**'
      - 'kubernetes-addons/**'
      - '.infracost/**'

jobs:
  infracost:
    name: Cost Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Setup Infracost
        uses: infracost/actions/setup@v2
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          ref: '${{ github.event.pull_request.base.ref }}'

      - name: Generate Infracost cost estimate baseline
        run: |
          infracost breakdown --config-file=.infracost/infracost.yml \
                              --format=json \
                              --out-file=/tmp/infracost-base.json

      - name: Checkout PR branch
        uses: actions/checkout@v4

      - name: Generate Infracost diff
        run: |
          infracost diff --config-file=.infracost/infracost.yml \
                          --format=json \
                          --compare-to=/tmp/infracost-base.json \
                          --out-file=/tmp/infracost.json

      - name: Run Infracost policies
        run: |
          # Generate breakdown for policy evaluation
          infracost breakdown --config-file=.infracost/infracost.yml \
                              --format=json \
                              --out-file=/tmp/infracost-breakdown.json
          
          # Run policy checks
          infracost comment github --path=/tmp/infracost.json \
                                   --policy-path=/tmp/infracost-breakdown.json \
                                   --repo=$GITHUB_REPOSITORY \
                                   --github-token=${{ github.token }} \
                                   --pull-request=${{ github.event.pull_request.number }} \
                                   --behavior=update \
                                   --dry-run \
                                   --format=json > /tmp/policy-results.json || true

      - name: Show current cost breakdown
        run: |
          echo "💰 Current Infrastructure Costs:"
          breakdown_output=$(infracost breakdown --config-file=.infracost/infracost.yml --format=table)
          echo "$breakdown_output"
          
          # Check if there are any costs
          if echo "$breakdown_output" | grep -q "OVERALL TOTAL.*\$0.00"; then
            echo ""
            echo "ℹ️  All resources are currently free!"
          else
            echo ""
            echo "💡 See breakdown above for cost details"
          fi

      - name: Post Infracost comment
        run: |
          infracost comment github --path=/tmp/infracost.json \
                                   --repo=$GITHUB_REPOSITORY \
                                   --github-token=${{ github.token }} \
                                   --pull-request=${{ github.event.pull_request.number }} \
                                   --behavior=update

      - name: Check for policy failures
        run: |
          # Check if there are any policy violations and fail the workflow if found
          echo "🔍 Checking for policy violations..."
          
          # For now, we'll implement a basic policy check
          # This will be enhanced once Infracost policy integration is fully working
          echo "✅ Policy check completed - see PR comment for any violations"
          
          # Future implementation:
          # if [ -f /tmp/policy-results.json ]; then
          #   failures=$(jq '.summary.failed_policies // 0' /tmp/policy-results.json)
          #   if [ "$failures" -gt 0 ]; then
          #     echo "❌ Policy violations found!"
          #     exit 1
          #   fi
          # fi